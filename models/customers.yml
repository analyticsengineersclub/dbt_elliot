version: 2

models:
  - name: customers
    description: Table of cleaned up customer data including unique IDs, personal info, and purchase history
    columns:
      - name: customer_id
        description: Primary key of the model
        tests:
          - unique
          - not_null


sources:
  - name: coffee_shop
    project: analytics-engineers-club
    tables:
      - name: customers
        columns:
          - name: id
            tests:
              - unique
              - not_null

      - name: orders
        columns:
          - name: id
            tests:
              - unique
              - not_null